const base = require('airtable').base(process.env.AIRTABLE_BASE_ID);

/*------------------
      AIRTABLE
------------------*/

const at = {
  /*----
    Get
  ----*/
  async getRotations() {
    return;
  }
};

module.exports = at;